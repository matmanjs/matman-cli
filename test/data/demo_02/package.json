{
  "name": "21_basic",
  "version": "1.0.0",
  "description": "Mock service and Tester assistant",
  "main": "entry.js",
  "scripts": {
    "start": "node ./entry.js",
    "babel-dist": "babel ./src -d ./dist",
    "dev-win": "set NODE_ENV=development && pm2 start entry.js --name=01_basic",
    "dev-mac": "export NODE_ENV=development && pm2 start entry.js --name=01_basic",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "author": "",
  "license": "MIT",
  "dependencies": {
    "babel-runtime": "^6.26.0",
    "nightmare": "^3.0.1",
    "nightmare-handler": "^1.0.2",
    "url-handle": "^0.2.1"
  },
  "devDependencies": {
    "babel-preset-env": "^1.7.0",
    "babel-preset-stage-0": "^6.24.1"
  }
}
